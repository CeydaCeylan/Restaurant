@model IEnumerable<Malzeme>

<div class="content-body">
    <div class="container-fluid">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">MALZEME LİSTESİ</h4>
                    <a asp-controller="Malzeme" asp-action="MalzemeEkle" asp-route-id="0" class="btn btn-primary btn-rounded me-3 mb-2"><i class="fas fa-user-friends me-2"></i>+ Malzeme Ekle</a>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover table-responsive-sm">
                            <thead>
                                <tr>
                                    <th class="text-center">ID</th>
                                    <th class="text-center">Ad</th>
                                    <th class="text-center">Tür</th>
                                    <th class="text-center">Fiyat</th>
                                    <th class="text-center">İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var malze in Model)
                                {
                                    <tr>
                                        <td class="text-center">@malze.Id</td>
                                        <td class="text-center">@malze.Ad</td>
                                        <td class="text-center">@malze.Turu</td>
                                        <td class="text-center">@malze.Fiyat</td>
                                        <td class="text-center">
                                            <div class="d-end">
                                                <a asp-action="MalzemeEkle" asp-route-id="@malze.Id" class="btn btn-primary shadow btn-xs sharp me-1"><i class="fas fa-pencil-alt"></i></a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>