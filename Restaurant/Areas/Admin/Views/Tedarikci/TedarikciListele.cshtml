@model IEnumerable<Tedarikci>

<div class="content-body">
    <div class="container-fluid">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="card-title">TEDARİKÇİ LİSTESİ</h4>
                    <a asp-controller="Tedarikci" asp-action="TedarikciEkle" asp-route-id="0" class="btn btn-primary btn-rounded me-3 mb-2"><i class="fas fa-user-friends me-2"></i>+ Tedarikçi Ekle</a>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover table-responsive-sm">
                            <thead>
                                <tr>
                                    <th class="text-center">ID</th>
                                    <th class="text-center">Ad Soyad</th>
                                    <th class="text-center">E Posta</th>
                                    <th class="text-center">Telefon</th>
                                    <th class="text-center">Adres</th>
                                    <th class="text-center">İşlemler</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var tedarik in Model)
                                {
                                    <tr>
                                        <td class="text-center">@tedarik.Id</td>
                                        <td class="text-center">@tedarik.Ad @tedarik.Soyad</td>
                                        <td class="text-center">@tedarik.Eposta</td>
                                        <td class="text-center">@tedarik.Telefon</td>
                                        <td class="text-center">@tedarik.Adres</td>
                                        <td class="text-center">
                                            <div class="d-end">
                                                <a asp-action="RolEkle" asp-route-id="@tedarik.Id" class="btn btn-primary shadow btn-xs sharp me-1"><i class="fas fa-pencil-alt"></i></a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>